name: Testing

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

env:
  FILE_NAME: "Arti"
  MY_PATH: "/sss/bbb"

permissions:
  contents: read
  

jobs:
  build:
   runs-on: windows-2019
   
   steps:    
  
    - name: SetArtifactName
      env:
        MY_PATH2: "/ggsd"
      run: |         
          echo $(Get-Date -Format dd.MM.yy) | Set-Variable DATE_NOW
          echo $DATE_NOW
          Set-Variable -Name ARTFC_NAME -Value ($DATE_NoW + "-Ext4Fsd")
          echo "filename: $ARTFC_NAME"
          echo "MY_PATH22: $env:MY_PATH2"          
          echo "MY_PATH22: ${{env.MY_PATH2}}"
          echo "filename3: ${{env.FILE_NAME}}"
          Set-Variable -Name {env.MY_PATH2} -Value ("/fsssg")
          $MY_PATH2="/fsssg"
          ${ext:MY_PATH2}="/fsssjkgjg"
          echo "MY_PATH2: $MY_PATH2"
          echo "MY_PATH22: ${env:MY_PATH2}"
          echo "MY_PATH22v2: ${{env.MY_PATH2}}"
          
    - name: Show Variables
      run: 'dir Env:'
      
    - name: Upload Artifact
      uses: actions/upload-artifact@v3
      with:
          name: ${{env.FILE_NAME}}
          path: |
            ${{env.MY_PATH}}
            ${{env.MY_PATH2}}             
